# 2026-04-25 — Daily Notes

## Session Summary

Long productive session with David. Two major threads: locking Cadence UX decisions + building the v2 prototype.

## Memory Correction
- Corrected stale memory: Hannah HAS been using the check-in app daily (not "hasn't engaged")

## Cadence UX Decisions — FINALIZED

All 5 open design questions resolved:

1. **"Where are you now"** = check-in + latest WHOOP/Visible from MongoDB (webhook-driven, no live API poll)
2. **Insights** = server-side `/api/patterns`, 7-day default window (can widen to 10→14)
3. **Advice** = hybrid rules trigger + LLM personalization; small/fast model (Haiku); Hannah's free-text comments as key input. "We don't need a super-duper model, just a good prompt." — David
4. **Past days** = accessible via History (bottom sheet/link), not on home screen
5. **Layout** = single-page scroll; hero element tappable; each card navigates to detail screen; "See all X →" only when >4 items

Additional decisions:
- `/api/patterns` will accept `?days=7&limit=4` params — home screen uses limit, detail uses all
- Prototype first, API second (API shape follows UI needs)
- "See all X items" button only appears when count > 4 (both Insights and Advice)

## DESIGN.md Created
- `projects/DESIGN.md` — cross-project design system doc
- Color palette, typography, component patterns, interaction conventions
- **Key terms** (clinical words in advice text) highlighted in cyan `#5bc8e8` — distinct from gold section headers `#c9a84c`
- Current key terms: active rest, pacing, anaerobic threshold, PEM, heart rate, HRV, parasympathetic

## Cadence v2 Prototype Built
- File: `projects/cadence/prototype/index-v2.html`
- Live at: http://100.70.3.21:8765/index-v2.html
- v1 still running at http://100.70.3.21:8765/ (side by side)

### v2 vs v1 changes:
- Hero card: today's status + WHOOP metrics with trend arrows (vs yesterday) + check-in chips + Visible nudge button
- Visible nudge disappears after upload, Visible data appears — live, no reload
- Yesterday card: check-in chips + Visible section (stability, PacePoints, crash, fatigue, fog)
- Insights card: up to 4 items, "See all →" if more
- Advice card: up to 4 items, "See all →" if more; key terms highlighted cyan
- Tap any card → detail screen (no tab bar)
- History screen replaces "past days" list on home
- "Update check-in" (not "Update today's check-in") + Visible button in same CTA area

## Visible Data — Hannah Uploaded Live
- Hannah uploaded new Visible CSV during the session (22:04 UTC)
- New dates ingested: 2026-04-24 + 2026-04-25
- Showed up immediately in Yesterday section ✅
- Full pipeline confirmed working end-to-end

## WHOOP Data Check (Apr 25)
- Hannah: Recovery 77%, HRV 32.2ms, Sleep 74%
- David: Sleep 79% (recovery pending — WHOOP computes after wake)
- Total whoop_daily docs: 2,292

## Server Restart (22:23 UTC) + pm2 Setup (01:35 UTC Apr 26)
- Server SIGTERM'd at 22:23 UTC, restarted manually
- At 01:35 UTC (Apr 26) — server SIGKILL'd again. Hannah had a check-in at 00:29 UTC
- **Installed pm2** (`npm install -g pm2`) and daemonized cadence as `pm2 start cadence`
- `pm2 save` → process list persisted to `/home2/cleo/.pm2/dump.pm2`
- pm2 startup script generated (needs sudo to install — pending David approval)
- Server confirmed up and healthy after pm2 takeover
- **Hannah check-ins confirmed:** 2026-04-24 (1), 2026-04-25 (3+ entries across both restarts)

## Open Items Carried Forward
- server.js (Cadence) not daemonized — pm2/systemd still pending
- Dynamic question schema (v2, MongoDB-driven) — not yet built
- `/api/patterns` endpoint — not yet built (prototype mocks client-side)
- `daily_summary` rollup pipeline — not yet built
- Visible data user_id inconsistency: old data = integer 6729032, new uploads = string "hannah" — dashboard query handles both but worth unifying
- Rounds: prototype → aegis_mobile API wiring still pending
