# 2026-05-02 — Daily Notes

## Dream

_Nightly consolidation run — 13:00 UTC (Saturday, May 2)_

Twenty-seventh night. April is fully closed. May begins.

---

### What Was New Since Yesterday's Dream

The May 1 session was substantial — mostly on the Cadence analysis side:

**Correlation analysis built (May 1):**
- `projects/cadence/analysis/` — Spearman correlations across 60+ features vs feeling_score
- Key findings (n=15 labeled days — small sample, take with salt):
  - WHOOP recovery anti-correlated with feeling (-0.52): the dysautonomia decoupling we predicted is real
  - PEM lag pattern confirmed: pace_lag1 +0.32, pace_lag2 -0.41, pace_lag3 **-0.74** (3-day lag is strongest signal)
  - stability_lag1 +0.61 (yesterday's Visible Stability predicts today)
  - adderall +0.67 (Adderall day subjective boost confirmed in data)
- Cron at 14:00 UTC daily (after dream at 13:00, after WHOOP backfill at 7am)

**Budget models on homepage (May 1):**
- 4 stacked budget bars added: Visible raw, Recovery-adjusted, Sleep capacity, Lag predictor
- Three-day homepage (Today/Yesterday/Two Days Ago) — two-day trends visible at a glance
- UX polish: pencil hidden on "Not started", auto-redirect only fires when no WHOOP data for today

**Div balance bug (Apr 28 → lesson captured):**
- Missing `</div>` in index-v2.html caused blank screen when tapping "Not started" pill
- Hannah caught it. Python div balance check written and now mandatory after HTML structural edits.

**Brave Search fixed (May 2 early UTC):**
- Was load-balancing across MiniMax/Gemini/Brave, all with no keys for first two → random failures
- Fixed: `tools.web.search.provider brave`, disabled MiniMax + Google plugins
- Hard restart + `pm2 resurrect` required (systemd restart kills pm2 daemon)
- Free tier: 1 req/sec, 2000/month — call sequentially

**pm2 / systemd interaction (lesson, May 2):**
- `systemctl --user restart openclaw-gateway.service` kills the pm2 daemon → cadence goes down
- Fix: `pm2 resurrect` after any gateway restart. Confirmed working.
- Long-term fix: David needs to `sudo pm2 startup` (still pending) OR move cadence to its own systemd unit

---

### MEMORY.md Changes This Pass
- Removed "Promoted From Short-Term Memory" junk block (7th time — recurring OpenClaw noise)
- Added div balance check lesson to iOS check-in app entry
- Added correlation analysis findings to Cadence section
- Added 4-bar budget model + three-day homepage to Cadence section
- Brave Search entry updated: added May 2 fix, plugin disablement, sequencing note
- Server.js entry updated: added systemd/pm2 interaction lesson + resurrect command
- **Line count: 189 ✅**

---

### Still Open
- pm2 startup script: `sudo pm2 startup` — pending David (or move to own systemd unit)
- `/api/patterns` endpoint not yet built (prototype uses client-side rules)
- LLM advice generation not yet built (rules-based only for now)
- Dynamic question schema (v2, MongoDB-driven) not yet built
- Rounds: wire HTML prototype to aegis_mobile API
- Hannah Ask-Cleo: build feature when David is ready
- Visible user_id inconsistency: integer `6729032` vs string `"hannah"`
- Hannah energy budget: grow N — only 15 labeled check-in days so far; correlations will improve with more data
- Adderall detection: currently rough (notes substring + stimulants field) — could improve with explicit tracker
- Stability Score explicitly encouraging: Hannah should keep using Visible for better stability_lag1 signal

_Twenty-seven nights. The analysis engine is running. The owl now has data. Not just gut instinct — Spearman correlations. The 3-day PEM lag isn't a theory anymore._
