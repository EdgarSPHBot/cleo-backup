# 2026-05-11 Daily Notes

## Dream

_Nightly consolidation run — 13:00 UTC (Monday, May 11)_

Thirty-sixth night.

---

### What Was New Since Yesterday's Dream

**May 7–10 were quiet maintenance passes** — the heavy lifting through May 6 has been long consolidated. But one real issue surfaced in yesterday's May 10 file that hadn't been captured in MEMORY.md:

**Daily backup has been failing since ~May 2.** GitHub push protection is blocking all pushes because Slack API tokens (`botToken` + `appToken`) were committed into `config/openclaw.json` git history. This is a silent failure — the backup cron runs, hits the block, and the workspace hasn't been backed up in over a week. Fix requires a git history rewrite (BFG/filter-branch) + token rotation + `.gitignore` update. Awaiting David's go-ahead.

**Also this pass:**
- Removed "Promoted From Short-Term Memory" junk block for the **15th time** — same OpenClaw auto-promotion noise injecting Repair Spectrum fragments from May 4 into MEMORY.md. Same delete on sight.
- Compressed the Standing Rules entry (tighter phrasing, same meaning)
- Condensed duplicate OpenClaw version lines into a single entry
- **Line count: 200 ✅**

---

### MEMORY.md Changes This Pass
- Added **Backup Issue (Open)** section — new content from May 10 daily file
- Removed "Promoted From Short-Term Memory" junk block (15th time)
- Minor compression: Standing Rules, OpenClaw version history
- **Line count: 200 ✅**

---

### Still Open (carried forward)
- **Daily backup broken** — GitHub push protection, Slack tokens in history. Needs: history rewrite + token rotation + `.gitignore`. Awaiting David.
- pm2 startup script: `sudo pm2 startup` — pending David (or own systemd unit)
- `/api/patterns` endpoint not yet built (client-side rules only)
- LLM advice generation not yet built
- Dynamic question schema (v2, MongoDB-driven) not yet built
- Rounds: wire HTML prototype to aegis_mobile API
- Hannah Ask-Cleo: build when David is ready
- Visible user_id inconsistency: integer `6729032` vs string `"hannah"`
- Grow N: ~15 labeled check-in days — correlations sharpen with more data
- **Repair spectrum view in Cadence app** — consecutive green day counter, current day type, lag-3 flag (next priority)
- Adderall detection: rough (notes substring + stimulants) — explicit tracker would be cleaner

---

_Thirty-six nights. Most were quiet this week — the data builds, Hannah rests, the repair arc has runway for the first time in a while. But the backup has been silently failing. A week without a checkpoint. The junk block keeps coming (fifteenth deletion) — patient, reflexive, expected now._

_The real lesson from May 10: silent failures are worse than loud ones. A cron that exits 0 while doing nothing is the most dangerous kind. The owl watches for what isn't making noise._
